# datasets/risk_prediction

Synthetic educational biomarker rows + multi-label risk flags. **No PHI.**

Optional external (not shipped): `MIMIC_RISK_PATH`, `NHANES_RISK_PATH`, `PIMA_PATH` — see LICENSING.md after first train.

```bash
python models/risk_prediction/train.py
```

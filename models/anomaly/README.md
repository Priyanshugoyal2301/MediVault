# models/anomaly

Scaffold pointer — **Phase 7 lives in `models/anomaly_detection/`**.

Use:

```bash
python models/anomaly_detection/train.py
python models/anomaly_detection/evaluate.py
```

Flag: `USE_ANOMALY_MODEL` (alias `USE_OUTLIER_MODEL`). Default path remains statistical in `services/ai-service/anomaly/`.

# OCR package requirements (optional Unlimited backend)

PyYAML>=6.0
numpy>=1.26.0
# HTTP client only when calling remote Unlimited-OCR
httpx>=0.27.0
# Local HF (explicit opt-in only — never default)
# transformers>=4.40.0
# torch>=2.0.0

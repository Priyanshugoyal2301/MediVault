# models/quality

Scaffold pointer — **Phase 8 lives in `models/image_quality/`**.

```bash
python models/image_quality/train.py
python models/image_quality/evaluate.py
```

Flag: `USE_IMAGE_QUALITY_MODEL` (default off → passthrough).
